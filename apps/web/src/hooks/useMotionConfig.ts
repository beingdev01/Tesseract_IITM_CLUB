// Stub — motion config is handled globally by framer-motion defaults.
export function useMotionConfig() {
  return {
    reducedMotion: false,
    isMobile: false,
    shouldReduceMotion: false,
    prefersReducedMotion: false,
    disableParallax: false,
  };
}
